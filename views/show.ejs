<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= data.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900 h-full w-full">
    <div class="w-full m-auto text-zinc-200 p-4 space-y-2">
      <h1 class="text-2xl font-bold"><%=data.title %></h1>
      <p class="text-base"><%=data.desc %></p>
      <div class="flex gap-4">
        <a
          href="/edit/<%=data._id%>"
          class="px-3 py-2 bg-blue-600 cursor-pointer rounded-sm"
          >Edit</a
        >
        <a
          href="/delete/<%=data._id%>"
          class="px-3 py-2 bg-blue-600 cursor-pointer rounded-sm"
          >Delete</a
        >
      </div>
    </div>
  </body>
</html>
